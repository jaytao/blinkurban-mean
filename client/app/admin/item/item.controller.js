'use strict';

angular.module('blinkUrbanApp')
  .controller('ItemCtrl', function ($scope) {
  	$scope.item = {};
  	
    $scope.addItem = function(form){
      if(form.$valid) {

      }
    };
  });
